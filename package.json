{
  "name": "instagram-logo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npx parcel src/index.html",
    "build": "rm -rf build && parcel build src/index.html -d build --public-url /instagram-logo/",
    "deploy": "gh-pages -d build"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "stylelint": "^13.10.0",
    "stylelint-config-standard": "^20.0.0"
  }
}
